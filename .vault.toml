# .vault.toml — vault-kit manifest for console
#
# NON-SECRET: this file contains only project identifiers and key NAMES.
# NEVER put secret values, client secrets, or passwords in this file.
# Commit this file to version control.
#
# Generated by sre-agent on 2026-07-02T20:49:37Z

vault_url = "https://vault.raxx.app"
env       = "dev"

[project]
id   = "c71477a0-a84f-4aec-bdc0-8b20ab946851"
name = "console"
slug = "console"

# Key NAMES this repo needs from the vault.
# rvault resolves values at runtime — they never touch disk.
keys = [
  "ANTHROPIC_API_KEY",
  "CLOUDFLARE_API_TOKEN",
  "CONSOLE_ADDR",
  "CONSOLE_DB",
  "CONSOLE_DISCORD_WEBHOOK_URL",
  "CONSOLE_LLM_PLUGIN",
  "CONSOLE_MODEL",
  "CONSOLE_NOTIFY_PLUGINS",
  "CONSOLE_PAGERDUTY_ROUTING_KEY",
  "CONSOLE_SLACK_WEBHOOK_URL",
  "CONSOLE_STATUS_PLUGINS",
  "CONSOLE_STORE_PLUGIN",
  "CONSOLE_WEBHOOK_SECRET",
  "CONSOLE_WEBHOOK_URL",
  "EMAIL_FROM",
  "EMAIL_TO",
  "HEROKU_API_KEY",
  "OLLAMA_HOST",
  "OPENAI_API_KEY",
  "SENTRY_AUTH_TOKEN",
  "SMTP_HOST",
  "SMTP_PASSWORD",
  "SMTP_PORT",
  "SMTP_USERNAME",
]
